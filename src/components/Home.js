import Profile from "./Profile";
import Projects from "../pages/Projects";

const Home = () => {
  return (
    <Profile
    // textEnter={textEnter}
    // textLeave={textLeave}
    // linkEnter={linkEnter}
    // cursorVariant={cursorVariant}
    // variants={variants}
    />
  );
};

export default Home;
